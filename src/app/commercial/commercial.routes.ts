export enum CommercialRoutes {
  Validation = 'validation',
  Settings = 'settings',
  Elements = 'elements',
  Add = 'add',
}

export enum ElementRoutes {
  Alert = 'alert',
  Modal = 'modal',
  Buttons = 'buttons',
  Tabs = 'tabs',
  DataTable = 'data-table',
  Forms = 'forms',
}

export enum SettingRoutes {
  Profile = 'profile',
  Users = 'users',
}
