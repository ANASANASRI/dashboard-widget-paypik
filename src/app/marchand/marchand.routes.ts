export enum MarchandRoutes {
    Dashboard = 'dashboard',
    More = 'dashboard/more',
    Support = 'support',
    Settings = 'settings',
    Elements = 'elements',
}

export enum SettingRoutes {
    Profile = 'profile',
    Users = 'users',
}

export enum SupportRoutes {
    FAQ = 'faq',
    Contact = 'contact',
}