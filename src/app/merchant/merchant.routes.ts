export enum MerchantRoutes {
    Dashboard = 'dashboard',
    More = 'dashboard/more',
    Events = 'events',
    Settings = 'settings',
    Elements = 'elements',
}

export enum ElementRoutes {
    Alert = 'alert',
    Modal = 'modal',
    Buttons = 'buttons',
    Tabs = 'tabs',
    DataTable = 'data-table',
    Forms = 'forms',
}

export enum SettingRoutes {
    Profile = 'profile',
    Users = 'users',
}
