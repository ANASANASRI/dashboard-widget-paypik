export enum MerchantRoutes {
    Dashboard = 'dashboard',
    More = 'dashboard/more',
    Events = 'events',
    Settings = 'settings',
    Elements = 'elements',
}

export enum SettingRoutes {
    Profile = 'profile',
    Users = 'users',
}